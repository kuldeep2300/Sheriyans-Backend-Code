<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Data Association</title>
    <link rel="stylesheet" href="/stylesheets/output.css" />
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
      <div class="flex justify-end">
        <a
          href="/logout"
          class="bg-red-500 px-3 py-2 rounded-md hover:bg-red-600"
          >Logout</a
        >
      </div>
      <h5 class="mt-2 mb-5">Update Your Post</h5>
      <form action="/edit/<%= post._id %>" method="post">
        <textarea
          name="content"
          id="post"
          class="block border-2 outline-none bg-zinc-900 border-zinc-600 resize-none w-1/3 text-white p-3 rounded-md"
          placeholder="What's on your mind ?"
        ><%= post.content %></textarea>
        <input
          type="submit"
          value="Update Your Post"
          class="block bg-yellow-500 text-black px-3 py-2 mt-2 rounded-md w-40 cursor-pointer  hover:bg-yellow-600"
        />
      </form>
    </div>
  </body>
</html>
